<h1>Listing books</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Author</th>
    <th>Is Available</th>
  </tr>

<% @books.each do |book| %>
  <tr>
    <td><%=h book.title %></td>
    <td><%=h book.author %></td>
    <td><%=h book.isAvailable %></td>
    <td><%= link_to 'Show', book %></td>
  </tr>
<% end %>
</table>

<br />
