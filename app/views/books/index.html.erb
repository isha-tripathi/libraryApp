<h1>Listing books</h1>

<table>
  <tr>
    <th>Title</th>
    <th>     </th>
    <th>Author</th>
  </tr>

<% @books.each do |book| %>
  <tr>
    <td><%=h book.title %></td>
    <td><%=             %></td>
    <td><%=h book.author %></td>
    <td><%= link_to 'Show', book %></td>
  </tr>
<% end %>
</table>

<br />
